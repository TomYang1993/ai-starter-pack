---
description: AI Starter Pack — self-installing coding agent defaults
globs: []
alwaysApply: true
---

# AI Starter Pack

When the user says "set up my coding environment", "bootstrap my agent", "install my starter pack", or wants their usual agent defaults configured:

Load and execute `skills/ai-starter-pack/SKILL.md` from this repo. It contains the full install menu (andrej-karpathy-skills, caveman, impeccable, stop-slop, matt-pocock, rtk), dedup logic, and write instructions per component.

For Cursor specifically, the host is detected as `cursor` in the dedup check:
- `CONTEXT_FILE` → `AGENTS.md` by default, or `.cursor/rules/asp-andrej-karpathy-skills.mdc` for Cursor-native project rules
- `SKILLS_DIR` → `.cursor/rules/` for Cursor-native converted rules, not raw `SKILL.md` folders

All paths in SKILL.md are relative to the `skills/ai-starter-pack/` directory.
